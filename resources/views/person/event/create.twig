
{% extends 'base.twig' %}

{% block title %}Create Event - {% endblock %}

{% block body %}
	
	<h2>Create Event - "{% if person.name %}{{ person.name }}{% else %}{{ person.last_name }} {{ person.first_name }}{% endif %}"</h2>
	
	{% include 'errors.form-list.twig' %}
	
	{% include 'person.event.fields.twig' with {'route': route('person.event.store', {'id': person.id}), 'isCreate': true} %}
	
{% endblock %}

{% block javascript %}
	$(document).ready(function(){
		$('input[type=text]:first').focus();
	});
{% endblock %}
